const { setDark } = require('./models/models.ejs');
const mongoose = require("mongoose");

describe('Dark Mode Test', () => {
	test('Should toggle the dark mode on/off', () => {
		expect(setDark).toBe();
		expect(setDark).toBe();
	})
});
